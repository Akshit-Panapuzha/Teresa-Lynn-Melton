# Copy of the variables the comments API needs. Set the real values in
# Vercel → Project → Settings → Environment Variables. Never commit a real token.
GITHUB_TOKEN=github_pat_xxx   # fine-grained token, this repo only, Contents: Read and write
GITHUB_REPO=Akshit-Panapuzha/Teresa-Lynn-Melton
GITHUB_BRANCH=main
COMMENTS_PATH=comments.json
